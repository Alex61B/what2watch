// lib/brand.ts
// Single source of truth for the product name. "PikFlix" is a working name and may change
// before launch — reference these constants instead of hardcoding the string so a rename is a
// one-line change rather than a repo-wide find-replace.
export const BRAND_NAME = 'PikFlix'
export const BRAND_TAGLINE = 'Where decisions get made'
